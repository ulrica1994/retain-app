import { Component } from '@angular/core';

@Component({
    // moduleId: module.id,
    selector: 'about-container',
    template: `
        <div class="about-container">
            <h1>About</h1>
        </div>
        `
})
export class About { }
