import { Component } from '@angular/core';

@Component({
    // moduleId: module.id,
    selector: 'auth-container',
    template: `
        <div class="auth-container">
            <h1>Auth Page</h1>
        </div>
        `
})
export class Auth { }
